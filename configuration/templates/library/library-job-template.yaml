- job-template:    
    id: "library-job-template"
    display-name: "{name}-library"
    description: "${description}"
    name: "{name}-library"
    folder: "{folder}"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/library-pipeline-dsl.groovy
    triggers:
      - github
