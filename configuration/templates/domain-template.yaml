- job-group:
    name: '{name}-domain'
    jobs:
      - 'folder-job-template'
      - '{name}-job-build':
         display-name: "{name}-domain"
      - 'pr-job-template':
         display-name: "{name}-pr"
      - '{name}-ci-deployment-template'

