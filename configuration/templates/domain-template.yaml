- job-group:
    name: '{name}-domain'
    jobs:
      - '{name}-job-folder'
      - '{name}-job-build':
         display-name: "{name}-domain"
      - 'pr-job-template':
         display-name: "{name}-pr"
      - '{name}-ci-deployment-template'

