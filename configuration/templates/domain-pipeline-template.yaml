- job-template:
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}-domain"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/domain-pipeline-dsl.groovy
    triggers:
      - github
