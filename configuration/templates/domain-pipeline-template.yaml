- job-template:
    id: "{name}-domain-template"
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/domain-pipeline-dsl.groovy
    triggers:
      - github
