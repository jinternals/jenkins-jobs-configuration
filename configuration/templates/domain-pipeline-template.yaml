- job-template:
    id: "{name}-domain-folder"
    name: "{name}"
    project-type: folder  
    
- job-template:    
    id: "{name}-domain-template"
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}-domain"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/domain-pipeline-dsl.groovy
    triggers:
      - github
    scm:
      - git:
         credentials-id: "github"
         url: "{gitRepository}"
         branches:
          - "origin/{gitBranch}"
