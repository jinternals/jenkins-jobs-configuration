- job-template:
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}-ci-pipeline"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/ci-pipeline-dsl.groovy
    parameters:
      - string:
          name: VERSION
