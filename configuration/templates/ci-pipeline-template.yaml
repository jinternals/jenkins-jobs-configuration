- job-template:
    id: "{name}-ci-pipeline"
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}-ci"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/ci-pipeline-dsl.groovy
    parameters:
      - string:
          name: VERSION
