- job-template:    
    id: "starter-job-template"
    display-name: "{name}-starter"
    description: "${description}"
    name: "{name}-starter"
    folder: "{folder}"
    project-type: pipeline
    dsl:
      !include-raw:
      - ../dsl/starter-pipeline-dsl.groovy
    triggers:
      - github
