- job-template:    
    id: "{name}-deployment-template"
    display-name: "{display-name}"
    description: "{description}"
    name: "{name}-ci-deployment"
    project-type: pipeline
    folder: "{folder}"
    environment: "{environment}"
    parameters:
      - string:
          name: VERSION
          description: "Application version."
    dsl:
      !include-raw:
      - ../dsl/ci-pipeline-dsl.groovy
