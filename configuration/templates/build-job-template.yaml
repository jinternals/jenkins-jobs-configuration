- job-template:    
    id: "build-job-template"
    display-name: "{display-name}"
    description: "${description}"
    name: "{name}-domain"
    project-type: pipeline
    folder: "{folder}"
    dsl:
      !include-raw:
      - ../dsl/domain-pipeline-dsl.groovy
    triggers:
      - github
