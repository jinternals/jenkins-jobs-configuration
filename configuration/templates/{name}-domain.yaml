- job-group:
    name: '{name}-domain'
    jobs:
      - '{name}-job-folder'
      - '{name}-job-build':
         display-name: "{name}-domain"
      - '{name}-job-pr':
         display-name: "{name}-pr"

