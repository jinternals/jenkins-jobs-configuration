- job-group:
    name: '{name}-domain'
    jobs:
      - '{name}-job-build':
         display-name: "{name}-domain"
      - '{name}-job-folder'
      - '{name}-job-pr':
         display-name: "{name}-pr"

